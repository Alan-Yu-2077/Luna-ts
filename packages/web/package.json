{
  "name": "@luna/web",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/Alan-Yu-2077/Luna-ts.git", "directory": "packages/web" },
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./index.html --production --outdir=dist && bun run build:assets",
    "build:assets": "cp public/live2dcubismcore.min.js dist/ && mkdir -p dist/models && cp -R public/models/. dist/models/ 2>/dev/null || true"
  },
  "dependencies": {
    "@luna/protocol": "workspace:*",
    "pixi-live2d-display": "0.5.0-beta",
    "pixi.js": "7.4.2",
    "zod": "^3.25.0"
  }
}
